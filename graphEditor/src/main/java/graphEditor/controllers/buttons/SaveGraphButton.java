package graphEditor.controllers.buttons;

import javax.swing.*;

public class SaveGraphButton extends JButton {

    public SaveGraphButton(Action a) {
        super(a);
        setText("Add a new vertex");
    }
}
