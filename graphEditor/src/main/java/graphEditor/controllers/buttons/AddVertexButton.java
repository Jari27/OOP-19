package graphEditor.controllers.buttons;

import javax.swing.*;

public class AddVertexButton extends JButton {

    public AddVertexButton(Action a) {
        super(a);
        setText("Add a new vertex");
    }
}
